services:
  pelicula_db:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: 1111
      POSTGRES_DB: pelicula_db
    ports:
      - "5002:5432"
    volumes:
      - ./data:/var/lib/postgresql/data


